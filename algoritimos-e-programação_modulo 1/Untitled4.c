#include <stdio.h>
#include <stdlib.h>

int main() {
int vetor[10], count;
for(count = 0; count < 10; count++){
vetor[count] = count;
}

for(count = 0; count < 10; count++){
printf ("%d foi armazenado no vetor.\n", vetor[count]);
}

}
//saída da execução
#include <stdio.h>
#include <stdlib.h>

int main() {
int vetor[10], count;
for(count = 0; count < 10; count++){
vetor[count] = count;
}

for(count = 0; count < 10; count++){
printf ("%d foi armazenado no vetor.\n", vetor[count]);
}

}
0 foi armazenado no vetor.
1 foi armazenado no vetor.
2 foi armazenado no vetor.
3 foi armazenado no vetor.
4 foi armazenado no vetor.
5 foi armazenado no vetor.
6 foi armazenado no vetor.
7 foi armazenado no vetor.
8 foi armazenado no vetor.
9 foi armazenado no vetor.

EXECUTAR SUCCESSFUL (tempo total: 154ms)
